# 复制为 .env 后按需修改
# cp .env.example .env

# 宿主机映射端口
MEMOS_HOST_PORT=5230

# 对外完整地址（协议+域名，不带末尾斜杠）
# 不设置时实例为私有模式；设置后保留公开访问能力
# MEMOS_INSTANCE_URL=https://memos.example.com

# 时区
TZ=Asia/Shanghai

# 仅 docker-compose.build.yml 使用
# MEMOS_VERSION=0.31.0-rc.1-dev
# MEMOS_COMMIT=6f45710a
